# QA Studio - Docker Environment Variables
# Copy this file to .env.local for local development overrides
# For production, use secure random values!

# Database
DB_PASSWORD=qastudio_dev_password

# Redis
REDIS_PASSWORD=qastudio_redis_pass

# MinIO (S3-compatible storage)
MINIO_USER=qastudio
MINIO_PASSWORD=qastudio_minio_pass

# Session Secret (CHANGE THIS IN PRODUCTION!)
# Generate with: openssl rand -base64 32
SESSION_SECRET=dev_secret_change_me_in_production

# Optional: Override these if needed
# SMTP_FROM=noreply@qastudio.local
# S3_BUCKET=qastudio
